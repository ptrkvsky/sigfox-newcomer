import styled from '@emotion/styled'

const BlockHeader = styled('header')`
  position: sticky;
  display: flex;
  align-items: center;
  min-height: 64px;
  padding: 0 16px;
  box-shadow: rgba(213, 218, 227, 0.6) 0px 4px 10px 0px;
`

export { BlockHeader }
